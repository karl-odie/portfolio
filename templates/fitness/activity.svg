<svg xmlns="http://www.w3.org/2000/svg" height="30" width="30">
    <polyline class="activity-polyline"
        points="
        {% for x, y in activity.svg_points %}
            {{x}}, {{y}}
        {% endfor %}
        "
        style="fill:none;stroke-width:1;stroke:black"
    />
</svg>
